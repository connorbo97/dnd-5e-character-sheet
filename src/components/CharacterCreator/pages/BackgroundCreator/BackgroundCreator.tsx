import styles from './backgroundCreator.module.scss';

type Props = any;
export const BackgroundCreator = (props: Props) => {
  return <div className={styles['container']}>BackgroundCreator</div>;
};
